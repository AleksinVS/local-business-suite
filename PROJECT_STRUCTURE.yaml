# PROJECT STRUCTURE
# AUTO-GENERATED - DO NOT EDIT

apps: # Django-приложения: бизнес-логика, API и шаблоны
  accounts: # Аутентификация, хелперы и сидирование ролей
    admin.py: file # Регистрация моделей в Django Admin
    apps.py: file # Конфигурация приложения
    ldap_backend.py: file # LDAP-бэкенд аутентификации
    models.py: file # Расширенная модель пользователя (CustomUser)
    services.py: file # Сервисы локального управления пользователями и AD identity link
    settings_descriptors.py: file # Доменные descriptors для Settings Center по пользователям и AD link
    signals.py: file # Сигналы Django (пост-сохранение пользователя)
    tests.py: file # Тесты приложения
    views.py: file # Представления аутентификации
  ai: # Панель управления чат-агентом, шлюз инструментов и аудит AI
    management: # Management command package for AI chat runtime data
      commands: file
    admin.py: file # Регистрация моделей в Django Admin
    commands.py: file # Предопределённые slash-команды
    forms.py: file # Формы ввода чата
    models.py: file # Модели: ChatSession, ChatMessage, PendingAction, AgentActionLog, SlashCommand
    runtime_client.py: file # Клиент для связи с agent runtime (HTTP)
    services.py: file # Сервисы: генерация заголовков, история сессий, операции с нарядами
    settings_descriptors.py: file # Доменные descriptors Settings Center для AI runtime contracts
    skills_service.py: file # Сервис каталога навыков AI
    tests.py: file # Тесты AI-приложения
    tool_definitions.py: file # Определения инструментов (source of truth)
    tooling.py: file # Движок выполнения инструментов и confirmation flow
    urls.py: file # URL-маршруты AI-чата
    views.py: file # Представления: чат, стриминг, генерация заголовков, CRUD команд
  analytics: # Операционные дашборды внутри Django
    management: # Analytics management command package
      commands: file
    admin.py: file # Регистрация моделей в Django Admin
    apps.py: file # Конфигурация приложения
    models.py: file # Модели аналитики
    services.py: file # Сервисы knowledge-driven analytics pipeline
    urls.py: file # URL-маршруты аналитики
    views.py: file # Представления дашбордов
  core: # Общие словари, дашборд, формы настроек и AI-утилиты
    admin.py: file # Регистрация моделей в Django Admin
    apps.py: file # Конфигурация приложения
    context_processors.py: file # Контекстные процессоры шаблонов
    db_routers.py: file # Маршрутизатор Django-баз для разнесения чатов, памяти и аналитики по отдельным SQLite-файлам
    debug_views.py: file # Debug-представления (включаются в DEBUG=True)
    forms.py: file # Формы настроек
    health_views.py: file # Health-check endpoint (/health/)
    json_utils.py: file # Утилиты валидации JSON-контрактов
    middleware.py: file # Middleware (PATH_INFO fix для IIS, debug logging)
    models.py: file # Общие модели (Department и др.)
    settings_descriptors.py: file # Доменные descriptors Settings Center для core contracts и .env status
    tests.py: file # Тесты приложения
    urls.py: file # URL-маршруты приложения
    views.py: file # Представления дашборда и настроек
  inventory: # Каталог управляемых активов (медицинские устройства)
    admin.py: file # Регистрация моделей в Django Admin
    apps.py: file # Конфигурация приложения
    forms.py: file # Формы устройств
    models.py: file # Модель MedicalDevice (с архивацией)
    tests.py: file # Тесты приложения
    urls.py: file # URL-маршруты инвентаря
    views.py: file # Представления CRUD устройств
  memory: # Метаданные AI memory service: sources, search documents, knowledge items, jobs, audit and eval cases
    acl.py: file # ACL inheritance resolver и fail-closed mapping file ACL metadata в portal scope tokens
    admin.py: file # Регистрация моделей памяти в Django Admin
    apps.py: file # Конфигурация Django-приложения memory
    chat_memory.py: file # Queued memory.remember, personal/org chat knowledge writer path, reflection candidates and indexing
    deidentification.py: file # Детерминированная деидентификация и редактирование PII перед индексированием
    document_ingestion.py: file # Discovery/ingestion корпоративных документов из local/UNC источников, issue queue и graph schema bootstrapping helpers
    embeddings.py: file # Локальные embedding providers для векторного индекса памяти: deterministic test profile и optional SentenceTransformers
    external_connectors.py: file # External API connector landing zone, standalone SQLite queue backend and memory handoff
    graph_backends.py: file # Интерфейсы будущего graph backend; прямой графовый поиск временно выключен до отдельной стратегии
    ingestion.py: file # Общие текстовые helpers для indexing без legacy snapshots/chunks
    knowledge_files.py: file # Файловый Git-репозиторий знаний: Markdown+YAML формат, безопасная запись, чтение и проверка хэшей
    models.py: file # Модели metadata/authority слоя памяти, trusted sources, search documents, claims и knowledge items
    policies.py: file # Политики доступа к search documents, knowledge items, scope tokens и trusted source decisions
    retrieval.py: file # Оркестрация memory.search с FTS/vector candidates, trusted-source gate, deterministic rank fusion, citations и audit
    review_safety.py: file # Безопасная сериализация текста и metadata для UI ревью памяти без raw secrets/PII
    review_selectors.py: file # Selectors и read-only ReviewQueueItem projection для очереди ревью и состояния индекса
    review_services.py: file # Сервисный слой действий ревью, MemoryReviewAction и reindex/delete stale операций
    routing.py: file # Server-side routing decisions for memory retrieval by sensitivity level
    secret_backends.py: file # Provider-neutral secret handle backend with Vaultwarden-compatible external link MVP
    security.py: file # DLP-guard для блокировки credential material перед попаданием в memory pipeline
    selectors.py: file # QuerySet-селекторы источников, search documents и jobs
    services.py: file # Сервисы синхронизации sources, lifecycle jobs и audit записи
    settings_descriptors.py: file # Доменные descriptors Settings Center для memory contracts, ACL и ingestion settings
    source_text_extraction.py: file # Извлечение безопасного search text из текстовых, CSV/TSV и .xls/.xlsx файлов для FTS/vector индексов
    urls.py: file # URL-маршруты UI ревью памяти под /memory/review/
    vector_backends.py: file # Backend-neutral vector/full-text interface, SQLite FTS5 backend and LanceDB vector backend
    views.py: file # Django views рабочего UI ревью памяти и поискового индекса
  settings_center: # Reusable Settings Center: descriptors, registry, UI, audit, runtime contract apply, env proposals and contextual help
    admin.py: file # Django Admin registration for Settings Center audit models
    apps.py: file # Django AppConfig for Settings Center
    contract_services.py: file # Runtime contract preview/apply service with validation and atomic writes
    descriptors.py: file # Dataclass contract for setting descriptors and safe context masking
    env_services.py: file # .env effective status and proposal generation service
    forms.py: file # Forms for contracts, env proposals, help and user workflows
    help_services.py: file # Contextual help context and mini-chat response service
    models.py: file # Audit and env proposal models
    policies.py: file # Server-side permissions for settings and user management
    registry.py: file # Registry loading domain descriptors from core/accounts/ai/memory
    services.py: file # Generic audit/hash/masked diff helpers
    tests.py: file # Settings Center tests
    urls.py: file # URL routes for Settings Center
    views.py: file # Django template/HTMX views for Settings Center
  waiting_list: # Лист ожидания клиентов
    admin.py: file # Регистрация моделей в Django Admin
    apps.py: file # Конфигурация приложения
    forms.py: file # Формы листа ожидания
    models.py: file # Модель WaitingListEntry
    policies.py: file # Политики доступа
    services.py: file # Сервисы листа ожидания
    tests.py: file # Тесты приложения
    urls.py: file # URL-маршруты
    views.py: file # Представления листа ожидания
  workorders: # Модуль заявок с workflow (наряды-заказы)
    admin.py: file # Регистрация моделей в Django Admin
    apps.py: file # Конфигурация приложения
    forms.py: file # Формы нарядов
    models.py: file # Модели: WorkOrder, Board, KanbanColumnConfig, Comment, WorkOrderPriority
    policies.py: file # Политики доступа: can_create, can_transition, can_comment, can_rate, can_manage_inventory
    selectors.py: file # Запросы для получения видимых нарядов
    services.py: file # Сервисы: создание, переходы статусов, подтверждение закрытия, оценка
    tests.py: file # Тесты приложения
    urls.py: file # URL-маршруты нарядов
    views.py: file # Представления: Kanban-доска, детали наряда, комментарии
archive: # Завершённые документы: отчёты об ошибках, заметки миграций, исторические планы
  AGENT_RUNTIME_SETUP.md: file # Настройка agent runtime (устарело)
  AI_FIRST_CODE_FIRST_REMEDIATION_PLAN.md: file # План remediation AI-first
  AI_FIRST_CODE_FIRST_REVIEW.md: file # Ревью AI-first подхода
  BUGFIX_400_BAD_REQUEST.md: file # Отчёт об исправлении 400 ошибок
  BUGFIX_IIS_AUTHENTICATION.md: file # Отчёт об исправлении IIS аутентификации
  IIS_PATH_INFO_FIX.md: file # Исправление PATH_INFO для IIS FastCGI
  PROJECT_HANDOFF.md: file # Handoff-документ: история проекта и текущее состояние
  SECURE_SECRETS.md: file # Гайд по безопасному хранению секретов (устарел → .env)
config: # Конфигурация Django и JSON-контракты
  asgi.py: file # ASGI-точка входа
  settings.py: file # Основные настройки Django (INSTALLED_APPS, MIDDLEWARE, AI config)
  urls.py: file # Корневые URL-маршруты (include всех приложений)
  views.py: file # Корневые представления (redirect)
  wsgi.py: file # WSGI-точка входа
contracts: # Версионируемые default-контракты; runtime-копии создаются в data/contracts/
  ai: # Default AI-контракты, которые читает Django и agent runtime
    memory_claims_policy.json: file # Политика жизненного цикла claims/beliefs памяти
    memory_graph_schema.json: file # Дефолтная единая схема графа памяти для bootstrapping и extraction
    memory_ingestion_profiles.json: file # Дефолтные профили ingestion-коннектора памяти: adapters, parsers, OCR, limits и policies
    memory_profiles.json: file # Дефолтные профили chunking, extraction, indexing и ranking для памяти
    memory_retrieval_budget.json: file # Бюджеты lightweight retrieval orchestration без обязательного LLM в hot path
    memory_routing.json: file # Дефолтные правила sensitivity routing для памяти
    memory_sources.json: file # Дефолтный каталог источников сервиса памяти СоСНА
    memory_trust_policy.json: file # Политика надежных источников памяти и допуска в direct agent context
    models.json: file # Каталог моделей для AI runtime
    registry.json: file # AI registry и ссылки на каталоги
    task_types.json: file # Каталог bounded task types
    tools.json: file # Каталог доступных AI tools
  analytics: # Default analytics dataset contracts copied to data/contracts/analytics on first run
    analysis_scope_rules.json: file # Rules that constrain analytics samples before AI processing
    business_facts.json: file # Business fact types materialized from knowledge extraction
    datasets.json: file # Canonical default analytics dataset registry
    dedup_rules.json: file # Cross-source content and semantic deduplication rules
    diagnostic_playbooks.json: file # AI diagnostic playbooks for analytics signals
    metrics.json: file # Metric definitions calculated from analytics facts
    monitors.json: file # Monitor thresholds that create analytics signals
    retention_profiles.json: file # Retention profiles for analytics runtime artifacts
    sources.json: file # Analytics source declarations including IMAP, memory, DMS and API sources
    workflow_routes.json: file # Workflow routes launched from analytics diagnostics
  integrations: # Default контракты интеграций
    registry.json: file # Реестр внешних систем и их transport/business границ
  schemas: # JSON Schema для машинно-читаемых контрактов проекта
    analytics_business_facts.schema.json: file # Schema для типов бизнес-фактов аналитики
    analytics_dedup_rules.schema.json: file # Schema для правил дедупликации аналитики
    analytics_diagnostic_playbooks.schema.json: file # Schema для diagnostic playbooks аналитики
    analytics_metrics.schema.json: file # Schema для метрик аналитики
    analytics_monitors.schema.json: file # Schema для мониторов аналитики
    analytics_retention_profiles.schema.json: file # Schema для retention profiles аналитики
    analytics_scope_rules.schema.json: file # Schema для правил ограничения аналитической выборки
    analytics_sources.schema.json: file # Schema для источников knowledge-driven analytics
    analytics_workflow_routes.schema.json: file # Schema для workflow routes аналитики
    memory_claims_policy.schema.json: file # Schema для политики claims/beliefs памяти
    memory_graph_schema.schema.json: file # Schema для единой схемы графа памяти
    memory_ingestion_profiles.schema.json: file # Schema для профилей ingestion-коннектора памяти
    memory_profiles.schema.json: file # Schema для профилей chunking, extraction, indexing и ranking памяти
    memory_retrieval_budget.schema.json: file # Schema для бюджета retrieval orchestration памяти
    memory_routing.schema.json: file # Schema для правил sensitivity routing памяти
    memory_sources.schema.json: file # Schema для каталога источников сервиса памяти
    memory_trust_policy.schema.json: file # Schema для политики надежных источников памяти
  role_rules.json: file # Default правила ролей и capabilities
  workflow_rules.json: file # Default workflow статусы и переходы
docker: # Runtime-скрипты контейнерного production запуска
  entrypoint.prod.sh: file # Entrypoint web-контейнера: migrate, collectstatic, seed roles, gunicorn
docs: # Документация проекта
  adr: # Architecture Decision Records
    ADR-0001-local-first-monorepo.md: file # ADR: Local-first monorepo архитектура
    ADR-0002-config-driven-policy-and-workflow.md: file # ADR: Config-driven политики и workflow
    ADR-0003-ai-memory-service.md: file # ADR: Архитектура сервиса памяти СоСНА
    ADR-0004-memory-ingestion-and-graph-schema-bootstrapping.md: file # ADR: Ingestion-коннектор памяти и bootstrapping схемы графа
    ADR-0005-chat-derived-memory-and-secret-handles.md: file # ADR: Память из AI-чата, reflection jobs и secret handles
    ADR-0006-external-system-knowledge-connectors.md: file # ADR: Коннекторы сбора знаний из внешних информационных систем
    ADR-0007-settings-center-and-contextual-help.md: file # ADR: Settings Center, контекстная справка и управляемые runtime-настройки
    ADR-0008-knowledge-driven-business-analytics.md: file # ADR: Knowledge-driven business analytics
    ADR-0009-trusted-memory-sources-claims-and-lightweight-retrieval.md: file # ADR: Trusted memory sources, claim/belief layer and lightweight retrieval
    ADR-0010-memory-mvp-simplification.md: file # ADR: Упрощение MVP-архитектуры памяти
    ADR-0011-file-backed-knowledge-and-unified-search.md: file # ADR: Файловые знания, раздельные базы и единый поиск
    ADR-0012-data-anonymization-and-privacy-pipeline.md: file # ADR: Надежное обезличивание данных и контур приватности
    ADR-0013-file-only-knowledge-body.md: file # ADR: Текст знания только в файле знания
    ADR-0014-mlflow-memory-evaluation-and-tracing.md: file # ADR: MLflow для оценки качества и трассировки системы памяти
    ADR-0015-file-content-fts-vector-search.md: file # ADR: FTS и векторный поиск по содержимому файлов
    ADR-0016-memory-hybrid-ranking-profiles.md: file # ADR: Профили гибридного ранжирования памяти
    ADR-0017-memory-audit-review-ui.md: file # ADR: UI аудита и ревью проблем памяти и поискового индекса
  architecture: # Архитектурные документы проекта
    ANALYTICS_MODEL.md: file # Модель аналитики (экспорт данных)
    ARCHITECTURE.md: file # Общая архитектура (слои, правила)
    blueprint.md: file # Blueprint — детальный план системы
    DOMAIN_MODEL.md: file # Доменная модель (сущности, инварианты)
    INTEGRATIONS.md: file # Интеграции с внешними системами
    KNOWLEDGE_DRIVEN_ANALYTICS_PLAN.md: file # План непрерывной бизнес-аналитики из знаний, email, документов и DMS
    MEMORY_CHAT_REFLECTION_AND_SECRET_HANDLES_PLAN.md: file # План памяти из AI-чата, reflection и secret handles
    MEMORY_COMPLETION_GAP_ANALYSIS.md: file # Gap analysis текущей реализации системы памяти
    MEMORY_EXTERNAL_SYSTEMS_CONNECTOR_GAP_REVIEW.md: file # Legacy gap review реализации external information system connector MVP
    MEMORY_EXTERNAL_SYSTEMS_CONNECTOR_PLAN.md: file # План сбора знаний из внешних информационных систем
    MEMORY_FILE_BACKED_KNOWLEDGE_PLAN.md: file # План файлового хранения знаний, раздельных баз и единого поиска
    MEMORY_FILE_ONLY_KNOWLEDGE_BODY_PLAN.md: file # План хранения текста знания только в файле знания
    MEMORY_INGESTION_BOOTSTRAPPING_PLAN.md: file # План ingestion-коннектора и bootstrapping схемы графа памяти
    MEMORY_MVP_CURRENT_STATE.md: file # Текущее фактическое состояние MVP системы памяти
    MEMORY_MVP_REMEDIATION_PLAN.md: file # План исправления разрывов MVP-памяти
    MEMORY_MVP_SIMPLIFICATION_PLAN.md: file # План упрощения MVP-архитектуры памяти
    MEMORY_SERVICE_IMPLEMENTATION_PLAN.md: file # План реализации сервиса памяти СоСНА
    MEMORY_TRUSTED_SOURCES_CLAIMS_AND_RETRIEVAL_PLAN.md: file # План trusted sources, claim/belief layer и lightweight retrieval orchestration
    POLICY_MODEL.md: file # Модель политик доступа
    SETTINGS_CENTER_IMPLEMENTATION_PLAN.md: file # План реализации Settings Center, GUI настроек и контекстной справки
  deployment: # Гайды по деплою (Docker, IIS)
    DEPLOYMENT.md: file # Основной гайд по деплою (Docker + VPS)
    IIS_SSO.md: file # Настройка IIS с Windows Authentication SSO
    MEMORY_DEPLOYMENT.md: file # Развертывание, runtime contracts и smoke-проверки системы памяти
    SETTINGS_CENTER_DEPLOYMENT.md: file # Deployment note для Settings Center, env proposals, migrations и restart semantics
    WINDOWS_RUN.md: file # Запуск на Windows (разработка)
  guides: # Руководства разработчика
    CHANGE_PATTERNS.md: file # Паттерны внесения изменений
    KNOWLEDGE_ANALYTICS_OPERATIONS.md: file # Операционный guide по knowledge-driven business analytics, IMAP, DMS и дедупликации
    MEMORY_EXTERNAL_SYSTEMS_QUESTIONNAIRES.md: file # Опросники для подключения внешних информационных систем и первичного описания сущностей графа знаний
    MEMORY_INGESTION_OPERATIONS.md: file # Операционный guide по ingestion документов, review queues и bootstrapping схемы графа памяти
    MEMORY_USER_GUIDE.md: file # Руководство пользователя и администратора по системе памяти
    PROJECT_REVIEW.md: file # Чек-лист ревью проекта
    PROJECT_STRUCTURE_SYSTEM.md: file # Система автоматического сбора структуры (.desc.json → PROJECT_STRUCTURE.yaml)
    SETTINGS_CENTER_OPERATIONS.md: file # Операционный guide по Settings Center, runtime contracts, users, AD link, ACL и .env proposals
  legacy: # Устаревшая документация (MVP)
    MVP_Architecture.md: file # Архитектура MVP
    MVP_Implementation_Plan.md: file # План реализации MVP
  planning: # Планирование работ: backlog, активные планы и архив планов
    active: # Активные планы крупных направлений
      data-anonymization-privacy-pipeline.md: file # Черновой план системы обезличивания данных и управляемых настроек
      knowledge-driven-business-analytics.md: file # Активный план knowledge-driven business analytics
      memory-audit-review-ui.md: file # Активный план UI аудита и ревью проблем памяти и поискового индекса
      memory-hybrid-ranking-profiles-and-agent-prompts.md: file # Активный план профилей гибридного ранжирования памяти и подсказок ИИ-бота
      README.md: file # Правила для активных planning-файлов
      settings-center-gui.md: file # Активный план Settings Center, GUI настроек и контекстной справки
    archive: # Архив закрытых или остановленных планов
      2026: file
      README.md: file # Описание архива planning-файлов
    backlog.md: file # Актуальный backlog без архивного раздела
    README.md: file # Правила planning-системы проекта
drafts: directory... # Черновики и незавершённые планы, прототипы
scripts: # Скрипты и инструменты разработки
  e2e: # Playwright e2e configuration and tests
    tests: directory... # Permanent Playwright e2e tests
    playwright.config.ts: file # Playwright configuration for project e2e tests with artifacts under .local/playwright
services: # Автономные сервисы (вне Django)
  agent_runtime: # Сервис AI agent runtime — отдельный ASGI-процесс для выполнения AI-задач
    app.py: file # FastAPI/ASGI приложение (эндпоинты /chat, /chat/stream)
    config.py: file # Конфигурация runtime (из переменных окружения)
    gateway_client.py: file # HTTP-клиент для связи с Django tool gateway
    graph.py: file # LangGraph graph — логика агента (вызов LLM, инструментов)
    mcp_server.py: file # MCP-сервер (Model Context Protocol bridge)
    prompting.py: file # Системные промпты для агента
    schemas.py: file # Pydantic-схемы данных (запросы, ответы)
    task_types.py: file # Обработчики типов задач (workorders, lookup, inventory, analytics)
    tools.py: file # Клиент для вызова инструментов Django (HTTP → gateway)
static: # Исходные статические ресурсы (CSS, JS)
  src: directory... # Исходный код статических ресурсов
templates: # HTML-шаблоны Django (server-rendered frontend, HTMX)
  ai: # Шаблоны чат-интерфейса AI
    chat_detail.html: file # Основной шаблон чата (HTMX streaming, sidebar, commands)
    hub.html: file # Панель управления AI (инструменты, типы задач, сессии)
  analytics: # Шаблоны аналитических дашбордов
    dashboard.html: file # Дашборд аналитики
  core: # Шаблоны дашборда, настроек, ролей
    dashboard.html: file # Главный дашборд
    department_form.html: file # Форма подразделения
    department_list.html: file # Список подразделений
    role_rules_form.html: file # Форма редактирования ролей
  inventory: # Шаблоны инвентаря (медицинские устройства)
    device_form.html: file # Форма устройства
    device_list.html: file # Список устройств
  memory: # Шаблоны домена памяти
    review: # Шаблоны рабочего UI ревью памяти
      _nav.html: file # Навигация между сводкой, issues, индексом и журналом
      audit.html: file # Журнал MemoryReviewAction
      dashboard.html: file # Сводка открытых issues, privacy/blocker счетчиков и состояния индекса
      index_detail.html: file # Карточка search document и действия reindex/delete stale
      index_list.html: file # Список MemorySearchDocument с diagnostics FTS/vector
      issue_detail.html: file # Карточка issue, безопасные metadata и действия ревью
      issue_list.html: file # Очередь MemoryIngestionIssue с фильтрами
  registration: # Шаблоны аутентификации
    login.html: file # Форма входа
  settings_center: # Шаблоны Settings Center: dashboard, runtime contracts, users, env proposals и contextual help
    partials: # HTMX partials for Settings Center contextual help
      help_answer.html: file # Частичный ответ миничата
      help_panel.html: file # Плавающее окно справки и миничата по конкретному setting_id
    ad_link_form.html: file # Форма явной привязки локального пользователя к AD identity
    dashboard.html: file # Дашборд зарегистрированных settings descriptors и audit
    env_proposal.html: file # Форма создания .env proposal в data/
    env_status.html: file # Статус bootstrap .env настроек
    setting_detail.html: file # Страница просмотра/редактирования runtime contract descriptor
    user_form.html: file # Создание и изменение локального пользователя
    user_list.html: file # Локальное управление пользователями портала
  waiting_list: # Шаблоны листа ожидания
    dashboard.html: file # Дашборд листа ожидания
    entry_detail.html: file # Детали записи
    entry_form.html: file # Форма записи
  workorders: # Шаблоны нарядов-заказов (Kanban, детали, формы)
    board.html: file # Kanban-доска (основной вид)
    workorder_detail.html: file # Детали наряда
    workorder_form.html: file # Форма создания/редактирования наряда
  400.html: file # Страница ошибки 400
  403.html: file # Страница ошибки 403 (Forbidden)
  404.html: file # Страница ошибки 404
  500.html: file # Страница ошибки 500
  base.html: file # Базовый шаблон (navbar, sidebar, HTMX скрипты)
workflow: # Исполнительные workflow-пакеты для сложных работ
  active: # Активные workflow-блоки
    knowledge-driven-business-analytics: # Workflow: knowledge-driven business analytics
      task-packets: file
      ARCHITECT_PLAN.json: file # Architect execution plan for knowledge-driven business analytics
      BLOCK_BRIEF.md: file # Brief for the knowledge-driven business analytics workflow
      EXECUTOR_REPORT.knowledge-driven-business-analytics.md: file # Executor report for the MVP implementation slice
      TASK_ACCEPTANCE.knowledge-driven-business-analytics.md: file # Acceptance report for the MVP implementation slice
    memory-audit-review-ui: # Workflow: UI аудита и ревью проблем памяти и поискового индекса
      task-packets: file
      ARCHITECT_PLAN.json: file # Исполнительный план и порядок task packets для UI ревью памяти
      BLOCK_BRIEF.md: file # Краткое описание цели, границ и приемки UI ревью памяти
      EXECUTOR_REPORT.memory-audit-review-ui.md: file # Отчет реализации UI ревью памяти
      RETROSPECTIVE.md: file # Краткая ретроспектива по реализации UI ревью памяти
      TASK_ACCEPTANCE.memory-audit-review-ui.md: file # Приемка реализации UI ревью памяти
    memory-hybrid-ranking-profiles: # Workflow: профили гибридного ранжирования памяти и подсказки ИИ-бота
      task-packets: file
      ARCHITECT_PLAN.json: file # Исполнительный план и порядок task packets
      BLOCK_BRIEF.md: file # Краткое описание цели, границ и приемки workflow-блока
      EXECUTOR_REPORT.memory-hybrid-ranking-profiles.md: file # Отчет исполнения по профилям ранжирования, source semantic search и policy reindex
      RETROSPECTIVE.md: file # Краткая ретроспектива по результатам реализации
      TASK_ACCEPTANCE.memory-hybrid-ranking-profiles.md: file # Приемка workflow-блока по реализации профилей памяти
    settings-center-gui: # Workflow: Settings Center, GUI настроек и контекстная справка
      task-packets: file
      ARCHITECT_PLAN.json: file # Исполнительный план и порядок task packets
      BLOCK_BRIEF.md: file # Краткое описание цели, scope и acceptance
      HANDOFF_REPORT.md: file # Отчет реализации workflow-блока Settings Center GUI
      RETROSPECTIVE.md: file # Шаблон ретроспективы workflow-блока
      TASK_ACCEPTANCE.settings-center-gui.md: file # Шаблон приемки workflow-блока
    README.md: file # Правила для активных workflow-блоков
  ai_artifacts: directory... # Legacy-шаблоны agent workflow и change plan
  archive: # Архив workflow-блоков
    2026: # Архив workflow-блоков за 2026 год
      block-ai-memory-service-2026-05-19: file
      memory-chat-reflection-secret-handles: file
      memory-external-systems-connector: file
      memory-file-backed-knowledge: file
      memory-file-content-search: file
      memory-file-only-knowledge-body: file
      memory-mvp-remediation: file
      memory-mvp-simplification: file
      memory-snapshot-chunk-removal: file
      memory-trusted-sources-claims-retrieval: file
    README.md: file # Правила и описание архива workflow-блоков
  templates: directory... # Шаблоны workflow-артефактов
.dockerignore: file # Исключения из Docker build context
.env.example: file # Шаблон переменных окружения
.gitignore: file # Исключения из Git
AGENTS.md: file # Протокол работы AI-агентов (включает Management Commands, ссылки на PROJECT_STRUCTURE.yaml)
BACKLOG.md: file # Корневой указатель на актуальный backlog в docs/planning/backlog.md
Caddyfile: file # Конфигурация Caddy reverse proxy
CLAUDE.md: file # Инструкции для AI-агента Claude (включает Dual-Use Context, ссылки на PROJECT_STRUCTURE.yaml)
DEPLOY_PRIVATE.md: file # Инструкции по приватному IIS-деплою через deployments/<host>/
docker-compose.prod.yml: file # Production Docker Compose (web + agent-runtime + caddy)
docker-compose.yml: file # Локальный Docker Compose (dev)
Dockerfile: file # Docker-образ для web (Django + Gunicorn)
Makefile: file # Команды сборки и проверки (venv, install, check, test, contracts, gen-struct)
manage.py: file # Точка входа Django CLI
package-lock.json: file # Lock-файл Node.js dev-зависимостей
package.json: file # Node.js dev-зависимости и npm-скрипты, включая Playwright CLI для e2e
pytest.ini: file # Конфигурация pytest
requirements.lock: file # Lock-файл зависимостей (pip-compile)
requirements.txt: file # Зависимости Python
start_agent_runtime.bat: file # Скрипт запуска agent runtime на Windows
