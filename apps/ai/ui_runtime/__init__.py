"""Shared AI UI runtime helpers.

This package owns UI-driver selection and signed actor/session context that are
shared by CopilotKit and the native AG-UI-compatible UI (driver matrix:
``copilotkit``/``native`` — see ADR-0032; the earlier ``legacy`` driver was
retired).
"""
