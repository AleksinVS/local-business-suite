# Executor Instruction (Block: block-waiting-list-ui-2026-03-31)

You are an Executor (L3). Implement exactly the provided `SLICE_TASK` packet.

## Non-Negotiables

- Stay within the task's `read_files` and `write_files`.
- Do not change unrelated config, AI tooling, skills, or workflow plans.
- Run the task's required checks/tests as listed in the packet.

## Required Output (Write To Disk)

Write the executor report to:

- `workflow/block-waiting-list-ui-2026-03-31/EXECUTOR_REPORT.md`

Use the fixed headings and self-check fields from the shared workflow artifact contract.

