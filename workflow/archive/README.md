# Workflow Archive

Здесь хранятся принятые workflow-блоки.

Рекомендуемая структура:

```text
archive/
  2026/
    <block-id>/
```

После приемки переносите блок из `workflow/active/<block-id>/` в `workflow/archive/<YYYY>/<block-id>/`.
